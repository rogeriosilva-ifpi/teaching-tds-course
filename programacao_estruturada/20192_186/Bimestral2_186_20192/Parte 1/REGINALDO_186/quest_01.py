def programa():
    quant_corrida = int(input('Quantas corridas hoje motorista:  '))
    valor_corrida = float(input('valor da corrida em R$:  '))
    porcentagem_uber = valor_corrida * 0,25
    porcentagem_motorista = valor_corrida * 0,75
    for i in range():

programa()