def prr():
    q_corridas = int(input('Dgite quantas corridas ocorreram:'))
    v_total = 0
    
    for i in range(q_corridas):
        preço = float(input('Dite valores: '))
        if v_total = 0:
            v_total = preço + v_total
        



prr()
